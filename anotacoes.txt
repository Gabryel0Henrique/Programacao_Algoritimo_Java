PA - 14/08/2026 
Comando em Java

Ctrl + Shift + P
Procura algo tipo criar java, build java

Tipos de Dados:
real - double
inteiro - int
booleano - boolean

declarando variáveis: int x, y;
x = 123;
y= 456;

======================================================
		ENTRADA E SAIDA
System.out.print("""

essas 3 aspas permitem quebrar linha e escrever
""")
System.out.print = comando de print ->
ln = pula linha ( println )
\n - pula linha


	PRINT FORMATADO
printf(format: "texto,texto %.2f", variavel)
printf(format: "seu valor 1 é %.2f e o seu valor 2 é %.2f ", variavel1, variavel2)

%f - variável float (decimais)
caso quiser um numero x de casas decimais após a virgula use: %.NumeroF ( %.2f %.3f )
%d - variável int
%s - variável string
%b - variável boolean


comando leia

importar a entrada do usuário
	construindo
. import java.until.Scanner; antes do public class app
. Scanner <variavel scanner> = new Scanner(System.in); inicia o scanner
. <variavel scanner>.close(); fecha o scanner

	usando na pratica
. System.out.print("frase do escreval")
. <variável que ganha valor> = <variavel scanner>.nextTIPODEDADO(); pra string é nextLine


===========================================================
Switch Case

.Estrutura
switch (variavel com a resposta) {

    case valor1:
        // código se for valor1;
        break;

    case valor2:
        // código se for valor2;
        break;

    case valor3:
        // código se for valor3;
        break;

    default:
        // código se nenhum case for encontrado;
        break;
}
======================================
Operadores Relacionais

> Maior que
< Menor que
>= Maior igual
<= Menor igual
== Igual
!= Diferente
&& and
|| or
! not
======================================
if else

if(condicao){valor SE verdadeiro}else{}
